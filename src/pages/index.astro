---
import Layout from "../layouts/Layout.astro";
import LandingMenu from "../components/LandingMenu/LandingMenu";

export interface Props {
  links: {
    title: string;
    href: string;
    color: string;
    image: string;
    active: boolean;
  };
}

const {
  links = {
    Home: "/",
    About: "/about",
    Contact: "/contact",
  },
} = Astro.props;
---

<Layout title="METRA Magazine">
  <main>
    <div
      class="z-100 h-screen w-full left-0 top-0 fixed opacity-50 bg-gradient-radial from-transparent via-transparent to-black"
    >
    </div>
    <div
      class="fixed text-white text-8xl font-metra left-5 top-5 z-[1000] drop-shadow-lg"
    >
      METRA
    </div>
    <LandingMenu client:visible />
    <div class="fixed text-white text-lg bottom-3 right-3 z-100">
      © 2022 Metra Magazine
    </div>
  </main>
</Layout>
